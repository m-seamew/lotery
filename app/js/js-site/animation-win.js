const animationWin = lottie.loadAnimation({
  container: document.querySelector('.popup__animation--win'),
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: '/lottery/img/smile-win.json' // the path to the animation json  
});