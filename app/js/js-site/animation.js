const animation = lottie.loadAnimation({
  container: document.querySelector('.popup__animation'),
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: './img/smile.json' // the path to the animation json
});


